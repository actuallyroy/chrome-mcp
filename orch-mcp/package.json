{
  "name": "orch-mcp",
  "version": "0.2.0",
  "description": "MCP server that lets a master Claude Code session drive other (worker) Claude sessions in the same project — resume by name, send prompts, compact, all via the `claude` CLI under the hood.",
  "type": "module",
  "bin": {
    "orch-mcp": "dist/index.js"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8",
    "zod-to-json-schema": "^3.23.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
