{
  "name": "chrome-mcp-site",
  "version": "0.2.0",
  "private": true,
  "description": "Landing page + distribution for chrome-mcp",
  "scripts": {
    "build:mcp": "node scripts/build-mcp.mjs",
    "build:android": "node scripts/build-android-mcp.mjs",
    "build:macos": "node scripts/build-macos-mcp.mjs",
    "build:windows": "node scripts/build-windows-mcp.mjs",
    "prebuild": "npm run build:mcp && npm run build:android && npm run build:macos && npm run build:windows",
    "build": "next build",
    "dev": "next dev",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "^15.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
